name: '🔬 Run CodeQL'

on:
  push:
    branches: [ master ]
    paths-ignore:
      - '**/*.md'
  pull_request:
    branches: [ master ]
    paths-ignore:
      - '**/*.md'
  schedule:
    - cron: '24 2 * * 4'
  workflow_dispatch:
  workflow_call:

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/checkout-core
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"
      # Build the dependency chain once, then trace only this repository's rebuild.
      - run: dotnet build tests/Jellyfin.Plugin.Postgresql.Tests/Jellyfin.Plugin.Postgresql.Tests.csproj --configuration Release
      - uses: github/codeql-action/init@db488ddef3bf6cb639b32c2e9a7c0a7ea8271d28 # v4.37.8
        with:
          languages: csharp
          build-mode: manual
          queries: security-and-quality
      # A solution-wide Rebuild can clean the plugin's reference DLL while tests compile.
      - name: Rebuild plugin, then tests, for CodeQL
        run: |
          dotnet build Jellyfin.Plugin.Postgresql/Jellyfin.Plugin.Postgresql.csproj --configuration Release --no-restore --no-dependencies -t:Rebuild
          dotnet build tests/Jellyfin.Plugin.Postgresql.Tests/Jellyfin.Plugin.Postgresql.Tests.csproj --configuration Release --no-restore --no-dependencies -t:Rebuild
      - uses: github/codeql-action/analyze@db488ddef3bf6cb639b32c2e9a7c0a7ea8271d28 # v4.37.8
